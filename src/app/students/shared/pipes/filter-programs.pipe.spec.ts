import { FilterProgramsPipe } from './filter-programs.pipe';

describe('FilterProgramsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProgramsPipe();
    expect(pipe).toBeTruthy();
  });
});
